#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../text-to-svg/bin/text-to-svg" "$@"
  ret=$?
else 
  node  "$basedir/../text-to-svg/bin/text-to-svg" "$@"
  ret=$?
fi
exit $ret
